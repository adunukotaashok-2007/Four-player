function createRoom() {
    alert("Room creation will be added in the multiplayer system.");
}

function joinRoom() {
    let code = prompt("Enter your room code:");

    if (code) {
        alert("Joining room: " + code);
    }
}

function openGame(gameName) {
    alert(gameName + " will be added soon!");
}
